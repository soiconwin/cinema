/*
 * package Ptit.Cinema.test;
 * 
 * 
 * import static org.assertj.core.api.Assertions.assertThat;
 * 
 * import javax.transaction.Transactional; import org.junit.jupiter.api.Test;
 * import org.springframework.web.bind.annotation.ModelAttribute;
 * 
 * import Ptit.Cinema.model.Movie; import Ptit.Cinema.model.Room; import
 * Ptit.Cinema.model.Showtimes; import Ptit.Cinema.repository.MovieRepository;
 * import Ptit.Cinema.repository.RoomRepository; import
 * Ptit.Cinema.repository.ShowTimesRepository; import
 * Ptit.Cinema.service.MovieService; import Ptit.Cinema.service.RoomService;
 * import Ptit.Cinema.service.ShowtimesService;
 * 
 * @Transactional public class TestUpdateShowTimes {
 * 
 * ShowTimesRepository stRepo; MovieRepository mvRepo; RoomRepository rRepo;
 * 
 * ShowtimesService stService; RoomService roomService; MovieService
 * movieService;
 * 
 * // @Test // public void getShowTimes(@ModelAttribute("showtimesDTO")
 * ShowtimesDTO showtimesDTO) { // Showtimes showtimes = new Showtimes(); // //
 * showtimes.setId(100); // showtimesDTO.setMovieId(1); //
 * showtimesDTO.setRoomId(3); // showtimes.setFare(3); //
 * showtimes.setDay("2022-10-19T10:00"); // Showtimes st =
 * stRepo.save(showtimes); // ////
 * assertThat(st.getMovie().getId(1),st.getRoom().getId(3)); // // } }
 */